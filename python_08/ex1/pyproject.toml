[project]
name = "ex1"
version = "0.1.0"
description = "A program to analyze matrix data"
authors = [
    {name = "Your Name",email = "you@example.com"}
]
requires-python = ">=3.10"
dependencies = [
    "pandas (>=2.1,<3.0.0)",
    "numpy (>=1.26,<=2.2.6)",
    "matplotlib (>=3.8,<4.0.0)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
